import { lazy } from 'react'

export const Settings = lazy(() => import('./Settings'))

export const Activities = lazy(() => import('./Activities'))

export const Other = lazy(() => import('./Other'))